[project]
name = "lerobot_teleoperator_so101_keyboard"
version = "0.1.0"
description = "LeRobot plugin: keyboard teleop for SO-101 joint control"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
  "lerobot",
  "pynput>=1.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lerobot_teleoperator_so101_keyboard"]
