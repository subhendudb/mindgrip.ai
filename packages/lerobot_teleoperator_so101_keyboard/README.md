# LeRobot teleoperator plugin

Installable package `lerobot_teleoperator_so101_keyboard` — discovered
automatically by `register_third_party_plugins()`.

```bash
pip install -e .
# then:
lerobot-teleoperate --teleop.type=so101_keyboard --robot.type=so101_mujoco ...
```
